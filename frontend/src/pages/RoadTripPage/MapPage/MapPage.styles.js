import RoomRoundedIcon from '@material-ui/icons/RoomRounded';
import { withStyles } from '@material-ui/core';

const MapMarker = withStyles({
  root: {
    color: '#F76C6C',
    fontSize: '50px',
  },
})(RoomRoundedIcon);

export default MapMarker;
